## How to run the project

### Server:
1. Open the Server_API solution in Visual Studio
2. Run the project (https://localhost:44347)

### Client:
1. Open the Angular project
2. Run `npm install`
3. Run `ng serve`
4. Open http://localhost:4200